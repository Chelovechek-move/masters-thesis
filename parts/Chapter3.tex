\section{Нелинейная многоимпульсная задача встречи}

\subsection{Постановка задачи}

В предыдущей главе была рассмотрена линейная двухимпульсная задача встречи. Такая постановка удобна для быстрого поиска приближённого решения, однако в реальных задачах накладываются ограничения на величины импульсов скорости $\Delta V_{\text{max}} > 0$. Наличие таких ограничений не позволит разрешить весь набор стоящих перед нами задач, оставаясь в классе двухимпульсных решений. Поэтому необходимо рассматривать многоимпульсные решения. Для такого случая, по аналогии с (\ref{a_t_2_imp}), управляющее ускорение будет иметь вид:
\begin{equation*}
    \mathbf{a}(t) = \sum_{j=1}^N \delta(t - t_j) \Delta \mathbf{V}_j,
\end{equation*}
где $N$ --- число импульсных манёвров. Кроме того, точность линейного приближения может оказаться недостаточной при решении прикладных задач. Поэтому далее рассмотрим нелинейную многоимпульсную задачу встречи:
\begin{equation}
\begin{aligned}
    \min_{\Delta \mathbf{V}, \mathbf{L}} \quad &\sum_{j=1}^N \Vert \Delta \mathbf{V}_j \Vert_2, \\
    \text{s.t.} \quad &\dot{\mathbf{y}} = \mathbf{f}(t, \mathbf{y}) + \sum_{j=1}^N \delta(t - t_j) B(\mathbf{y}) \Delta \mathbf{V}_j, \\
    &\mathbf{y}(t_i) = \mathbf{y}_i, \\
    &\mathbf{y}(t_f) = \mathbf{y}_t, \\
    &\Vert \Delta \mathbf{V}_j \Vert_2 \leq \Delta V_{\text{max}}, \quad j = 1, \dots, N.
\end{aligned}
\label{problem_N_imp_non_linear}
\end{equation}
Задачу (\ref{problem_N_imp_non_linear}) будем решать в два этапа: поиск линейного начального приближения и его уточнение для удовлетворения нелинейных ограничений.

\subsection{Начальное приближение}

Начальное приближение строится по локально оптимальным решениям линейной двухимпульсной задачи встречи (\ref{problem_2_imp_linear_non_cond}). Для этого она решается методом Ньютона с множественным стартом при различных начальных приближениях по истинным долготам $L_1$ и $L_2$. Такие локально оптимальные решения представлены на рисунке \ref{fig::J_and_sols}. Возможность гибкого задания области поиска позволяет учесть ограничения на запрещённые для маневрирования витки уже на этом этапе. В результате получаем $K$ локально оптимальных двухимпульсных решений.

Далее найденные решения сортируются по возрастанию функционала (\ref{problem_2_imp_linear_non_cond}). На рисунке \ref{fig::J_and_sols} 50 самых оптимальных решений пронумерованы и выделены зелёным цветом. Затем они последовательно отбираются, начиная с решения с минимальной характеристической скоростью. Если хотя бы один манёвр очередного решения расположен по истинной долготе ближе минимально допустимого угла $l_{\text{min}}$ к манёвру уже оставленного решения, то оно отбрасывается. После этой процедуры остаётся набор $\mathbf{L}_q \in \mathbb{R}^{2}$ и $\Delta \mathbf{V}_q \in \mathbb{R}^{6}$, где $q = 1, \dots, Q$, $Q$ --- число допустимых решений.

\begin{figure}[h!] \centering
    \includegraphics[height=0.55\textheight]{J_and_sols.png}
    \caption{Локальные минимумы функции характеристической скорости}
\label{fig::J_and_sols}
\end{figure}

Из первых $P$ решений этого набора, где $P \leq Q$, формируется многоимпульсное начальное приближение. Так как каждое из двухимпульсных решений удовлетворяет линейному ограничению (\ref{constraint_lin_2_imp}), то их взвешенная комбинации с правильно подобранными коэффициентами $\alpha_q \geq 0$ может обладать таким же свойством:
\begin{equation}
    \sum_{q=1}^P \alpha_q \mathcal{M} (\mathbf{L}_q) \Delta \mathbf{V}_q = \Delta \mathbf{y}_f, 
    \quad
    \sum_{q=1}^{P} \alpha_q = 1.
\label{constraint_lin_N_imp}
\end{equation}
Условие (\ref{constraint_lin_N_imp}) представляет собой обобщение ограничения (\ref{constraint_lin_2_imp}) на многоимпульсный случай. Начальное приближение, в свою очередь, строится объединением двухимпульсных решений: вектор истинных долгот $\mathbf{L}^{(0)} \in \mathbb{R}^{2P}$ образуется последовательной записью компонент $\mathbf{L}_q$, а вектор импульсов скорости $\Delta \mathbf{V}^{(0)} \in \mathbb{R}^{6P}$ --- последовательной записью компонент $\alpha_q \Delta \mathbf{V}_q$, $q = 1, \dots, P$. Таким образом, начальное приближение содержит $N = 2P$ импульсов. 

Возможны различные стратегии выбора коэффициентов $\alpha_q$. Мы будем рассчитывать их жадно, максимизируя вклад каждого отдельного манёвра и, тем самым, минимизируя их количество:
\begin{equation*}
    \alpha_q = \min \left(
    1 - \sum_{m=1}^{q-1} \alpha_m, \;
    \beta \frac{\Delta V_{\max}}
    {\max \bigl( \|\Delta \mathbf{V}_{q, 1}\|_2,\; \|\Delta \mathbf{V}_{q, 2}\|_2 \bigr)}\right), \quad
    q = 1, \dots, P,
\end{equation*}
где параметр $\beta$ вводится для частичного учёта нелинейных эффектов уже на этапе построения начального приближения. 

%Обычно $\beta$ выбирается из соотношения 
%\begin{equation*}
%    \beta \sim \left( \frac{\Delta V}{V_{\text{ref}}} \right)^2,
%\end{equation*}
%где $\Delta V$ --- характерная скорость решения задачи (\ref{problem_2_imp_linear_non_cond}), а $V_{\text{ref}}$ --- характерная орбитальная скорость.

\subsection{Уточнение решения}
Решение нелинейной задачи (\ref{problem_N_imp_non_linear}) будем искать методом последовательной линеаризации, используя в качестве начального приближения построенное выше многоимпульсное решение $\left( \mathbf{L}^{(0)}, \Delta \mathbf{V}^{(0)} \right)$. На этапе уточнения истинные долготы приложения манёвров фиксируются: $\mathbf{L} = \mathbf{L}^{(0)}$. Таким образом, уточняются только векторы импульсов скорости. Это упрощение связано с тем, что начальное приближение уже построено на основе локально оптимальных решений линейной задачи, поэтому основная структура многоимпульсного решения считается найденной. Нелинейный этап используется для компенсации ошибки, возникающей при переходе от линейной модели к полной модели движения. Важно отметить, что и в таком случае на каждой итерации будет пересчитываться опорная траектория $\mathbf{y}(t, \Delta \mathbf{V}^{(k)})$, соответствующая текущему вектору импульсов скорости $\Delta \mathbf{V}^{(k)}$. Невязка ограничения на $k$-й итерации может быть записана в следующем виде
\begin{equation*}
    \delta \mathbf{y}_f = \mathbf{y}_t - \mathbf{y}(t_f, \Delta \mathbf{V}^{(k)}).
\end{equation*} 
Линеаризация конечного состояния в окрестности $\Delta \mathbf{V}^{(k)}$ даёт
\begin{equation*}
    \mathbf{y}(t_f, \Delta \mathbf{V}^{(k)} + \delta \mathbf{V}^{(k)}) \approx  \mathbf{y}(t_f, \Delta \mathbf{V}^{(k)}) + \mathcal{M}^{(k)} (\mathbf{L}) \delta \mathbf{V}^{(k)}.
\end{equation*}
Тогда поправку $\delta \mathbf{V}^{(k)}$ можно определить из решения выпуклой подзадачи
\begin{equation}
\begin{aligned}
    \min_{\delta \mathbf{V}^{(k)}} \quad &\sum_{j=1}^N \Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2, \\
    \text{s.t.} \quad &\mathcal{M}^{(k)} (\mathbf{L}) \delta \mathbf{V}^{(k)} = \delta \mathbf{y}_f^{(k)}, \\
    &\Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2 \leq \Delta V_{\text{max}}, \quad j = 1, \dots, N.
\end{aligned}
\label{problem_N_imp_non_linear_dv}
\end{equation}
Подзадача (\ref{problem_N_imp_non_linear_dv}) является конической задачей второго порядка. С помощью введения вспомогательных переменных $t^{(k)}_j$, её можно записать в стандартном виде \cite{lobo1998applications}:
\begin{equation}
\begin{aligned}
    \min_{\mathbf{x}^{(k)}} \quad &\sum_{j=1}^N t^{(k)}_j, \\
    \text{s.t.} \quad 
    &\begin{bmatrix} 
        \mathcal{M}^{(k)} (\mathbf{L}) & \mathbf{0}_{6 \times N}
    \end{bmatrix} \mathbf{x}^{(k)} = \delta \mathbf{y}_f^{(k)}, \\
    &\Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2 \leq t^{(k)}_j, \quad j = 1, \dots, N, \\
    &0 \leq t^{(k)}_j \leq \Delta V_{\text{max}}, \quad j = 1, \dots, N,
\end{aligned}
\label{problem_N_imp_non_linear_dv_SOCP}
\end{equation}
где $\mathbf{x}^{(k)} = \left( \delta {\mathbf{V}_j^{(k)}}^\top, \; {\mathbf{t}^{(k)}}^\top \right)^\top \in \mathbb{R}^{4N}$. Задача (\ref{problem_N_imp_non_linear_dv_SOCP}) может быть решена, например, методом внутренней точки, реализованным в открытой библиотеке ALGLIB \cite{bochkanov_alglib}. После нахождения поправки выполняется обновление 
\begin{equation*}
    \Delta \mathbf{V}^{(k+1)} = \Delta \mathbf{V}^{(k)} + \delta \mathbf{V}^{(k)}.
\end{equation*}
Итерационный процесс продолжается до тех пор, пока невязка $\Vert \delta \mathbf{y}_f^{(k)} \Vert_2$ не станет меньше заданного порога. Таким образом, исходная нелинейная задача решается последовательностью выпуклых подзадач, соответствующих линейной аппроксимации ограничения в окрестности текущего приближения.

В ходе численного тестирования было установлено, что SOCP-подзадача (\ref{problem_N_imp_non_linear_dv_SOCP}) решается устойчиво: отказов решателя при корректно заданных входных данных обнаружено не было. Однако итерационная схема, основанная на задаче (\ref{problem_N_imp_non_linear_dv}), оказалась недостаточно надёжной. Наиболее частая проблема состояла в осцилляции невязки между двумя близкими значениями, а также в немонотонном изменении нормы $\Vert \delta \mathbf{y}_f^{(k)} \Vert_2$

Причина состоит в том, что функционал (\ref{problem_N_imp_non_linear_dv}) не является строго выпуклым. Поэтому подзадача может иметь несколько оптимальных решений. При переходе к следующей итерации меняется опорная траектория $\mathbf{y}(t, \Delta \mathbf{V}^{(k)})$, матрица $\mathcal{M}^{(k)} (\mathbf{L})$ и набор активных ограничений, что приводит к скачкообразному изменению найденной поправки.

Для устранения этой проблемы была рассмотрена регуляризованная постановка:
\begin{equation}
\begin{aligned}
    \min_{\delta \mathbf{V}^{(k)}} \quad &\sum_{j=1}^N \Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2 + 
    \frac{\rho}{2} \sum_{j=1}^N \Vert \delta \mathbf{V}_j^{(k)} \Vert_2^2, \\
    \text{s.t.} \quad &\mathcal{M}^{(k)} (\mathbf{L}) \delta \mathbf{V}^{(k)} = \delta \mathbf{y}_f^{(k)}, \\
    & \Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2 \leq \Delta V_{\text{max}}, \quad j = 1, \dots, N.
\end{aligned}
\label{problem_N_imp_non_linear_dv_rho}
\end{equation}
При $\rho > 0$ целевая функция становится сильно выпуклой по $\delta \mathbf{V}^{(k)}$. Поэтому, если допустимое множество непусто, регуляризованная подзадача (\ref{problem_N_imp_non_linear_dv_rho}) имеет единственное решение. При малых значениях $\rho$ такая регуляризация должна лишь устранять неоднозначность выбора поправки, практически не изменяя исходный критерий минимизации суммарной характеристической скорости. Однако численные эксперименты показали, что слабой регуляризации недостаточно для устойчивой сходимости. Даже в простых задачах со слабой нелинейностью требовались значения $\rho$ порядка единицы, а при увеличении продолжительности встречи или числа манёвров — значения порядка $10–100$. В этом режиме квадратичный член уже нельзя считать малой добавкой: он существенно влияет на положение минимума и фактически заставляет алгоритм искать решение в окрестности текущего приближения.

В отсутствии активных ограничений неревенств, это наблюдение позволяет заменить задачу (\ref{problem_N_imp_non_linear_dv_rho}) боле простой постановкой:
\begin{equation}
\begin{aligned}
    \min_{\delta \mathbf{V}^{(k)}} \quad &\sum_{j=1}^N \Vert \delta \mathbf{V}_j^{(k)} \Vert_2^2, \\
    \text{s.t.} \quad &\mathcal{M}^{(k)} (\mathbf{L}) \delta \mathbf{V}^{(k)} = \delta \mathbf{y}_f^{(k)}.
\end{aligned}
\label{problem_least_squares}
\end{equation}
Задача (\ref{problem_least_squares}) не минимизирует характеристическую скорость напрямую.Её смысл состоит в нахождении минимального изменения уже построенного многоимпульсного решения, необходимого для компенсации нелинейной невязки.

Так как $3N \geq 6$, то система $\mathcal{M}^{(k)} (\mathbf{L}) \delta \mathbf{V}^{(k)} = \delta \mathbf{y}_f^{(k)}$ является недоопределённой. Поэтому решение задачи (\ref{problem_least_squares}) может быть выражено через псевдообратную матрицу:
\begin{equation}
    \delta {\mathbf{V}^{(k)}}^* = \mathcal{M}^{(k)} \left( \mathcal{M}^{(k)} {\mathcal{M}^{(k)}}^\top \right)^{-1} \delta \mathbf{y}_f^{(k)}.
\label{problem_least_squares_sol}
\end{equation}
Решение (\ref{problem_least_squares_sol}) не учитывает ограничение на величину импульсов скорости $\Vert \Delta \mathbf{V}_j^{(k)} + \delta \mathbf{V}_j^{(k)} \Vert_2 \leq \Delta V_{\text{max}}, \quad j = 1, \dots, N$. Но оно может быть учтено неявно, заданием сооветствующего коэффициента $\beta$ при построении начального приближения. 

\subsubsection{Оценка коэффициента $\beta$}

Остаток линеаризации имеет второй порядок:
\begin{equation}
    R_{nl} = \mathcal{O} \left( \left( \frac{\Delta V}{V_{\text{ref}}} \right)^2 \right),
\end{equation}
где $\Delta V$ --- характерная скорость решения задачи, $V_{\text{ref}}$ --- характерная орбитальная скорость. Чтобы скомпенсирвоать этот остаток дополнительной поправкой $\delta V$, её линейное влияние должно иметь тот же порядок:
\begin{equation}
    \frac{\delta V}{V_{\text{ref}}} \sim C \left( \frac{\Delta V}{V_{\text{ref}}} \right)^2.
\end{equation}
Отсюда получаем оценку
\begin{equation}
    \delta V \sim C \frac{\Delta V^2}{V_{\text{ref}}}.
\end{equation}
Коэффициент $C$ учитывает нелинейность движения, длительность встречи, число манёвров и обусловленность матрицы $\mathcal{M}$. В хорошо обусловленных задачах он имеет порядок единицы; в сложных задачах может быть заметно больше.

Пусть максимальная величина начального импульса ограничена как
\begin{equation}
    \Delta V \leq \beta \Delta V_{\text{max}}.
\end{equation}
Тогда максимальная величина последующей поправки оценивается как
\begin{equation}
    \delta V \lesssim C \frac{\beta^2 \Delta V_{\text{max}}^2}{V_{\text{ref}}}.
\end{equation}
Чтобы ограничение на величину импульса не нарушалось после уточнения, достаточно потребовать выполнения
\begin{equation}
    \beta \Delta V_{\text{max}} + C \frac{\beta^2 \Delta V_{\text{max}}^2}{V_{\text{ref}}} \leq \Delta V_{\text{max}}.
\end{equation}
После деления на $\Delta V_{\text{max}}$ получаем
\begin{equation}
    \beta + C \frac{\beta^2 \Delta V_{\text{max}}}{V_{\text{ref}}} \leq 1.
\end{equation}
Обозначим $\mu = C \frac{\Delta V_{\text{max}}}{V_{\text{ref}}}$, тогда
\begin{equation}
    \mu \beta^2 + \beta - 1 \leq 0.
\end{equation}
Положительный корень соответствующего квадратного уравнения равен
\begin{equation}
    \beta_{\text{max}} = \frac{\sqrt{1 + 4 \mu} - 1}{2 \mu} = \frac{2}{1 + \sqrt{1 + 4 \mu}}.
\end{equation}
Поэтому практически можно выбирать
\begin{equation}
    \beta \leq \frac{2}{1 + \sqrt{1 + 4 C \frac{\Delta V_{\text{max}}}{V_{\text{ref}}}}}.
\end{equation}

\subsubsection{Ошибка решения}

В построенном алгоритме истинные долготы приложения манёвров зафиксированы линейным начальным приближением. Уточнение происходит только в пространстве импульсов скорости. 

 ЗДЕСЬ НУЖНО ОЦЕНИТЬ ВЕЛИЧИНУ ОШИБКИ РЕШЕНИЯБ КОТОРОЕ ПОЛУЧАЕТСЯ С ПОМОЩЬЮ АЛГОРИТМА ВЫШЕ, ВСЛЕДСТВИЕ ТОГО, ЧТО МЫ НЕ УТОЧНЯЕМЯС В ПРОСТРАНСТВЕ УГЛОВ ПРИЛОЖЕНИЯ ИМУЛЬСОВ СКОРОСТИ.

